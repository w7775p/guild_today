# 《今日公会照常营业》待办

更新日期：2026-08-31  
当前基线：V3 Task 1–8 已完成；Demo Readiness 审计修复已提交于 `a41e45e`。

## 状态说明

- `[ ]` 待处理
- `[>]` 进行中
- `[x]` 已完成并验证
- `NOT IMPLEMENTED` 尚未实现
- `UNVERIFIED` 已有部分实现，但缺少完整验证或正式策划确认

## V4 优先事项

### [ ] V4-01 冻结正式策划契约

- 为 Task 1 与 Task 8 补齐结果终态、奖励、关系/伤势/情报后果、后续任务 ID 和玩家可见报告文本。
- 将现有语义占位符替换为稳定 ID，并补充 Task 8 八条路线的金币与报告断言。
- 验收：正式资源不再依赖策划占位符；自动测试覆盖每条结果的终态、奖励和报告。

### [ ] V4-02 实现磁盘存档（`NOT IMPLEMENTED`）

- 建立 SaveSystem，保存并恢复日期、公会资源、角色伤势、关系、情报、任务生命周期、派遣和报告阅读状态。
- 冻结存档版本、唯一序列号、结算去重和失败恢复规则。
- 验收：新档、覆盖保存、读取恢复、损坏/缺失档案和版本不兼容均有自动测试。

### [ ] V4-03 接入持续后果消费者（`NOT IMPLEMENTED`）

- 让伤势、关系和情报参与后续任务展示、派遣校验或结果判定。
- 使用第二张及后续正式任务验证后果确实改变玩家可见选择与结果。
- 验收：同一任务在状态不同的存档中产生可验证的不同可玩路径。

### [ ] V4-04 建立内容导入与运行时 Schema（`NOT IMPLEMENTED / UNVERIFIED`）

- 评估并实现 JSON/Excel 导入、正式 Runtime Schema、`EventAsset` 和标准任务终态枚举。
- 保留稳定 ID、字段类型、引用闭包和错误定位；导入失败不得产生半成品资源。
- 验收：导入、校验、坏数据拒绝和正式资源回归测试全部通过。

### [ ] V4-05 完成人工 UI 验收与输入回归（`UNVERIFIED`）

- 在 Godot 编辑器中用真实鼠标、焦点切换和窗口尺寸变化检查任务文件、角色选择、禁用按钮、报告打开/关闭和状态刷新。
- 记录人工验收场景、操作步骤、结果和截图路径。
- 验收：正式主场景与第二张任务场景在目标窗口尺寸下无布局、焦点或输入阻塞。

### [ ] V4-06 冻结并执行 `TaskAsset.repeatable` 策略（`UNVERIFIED`）

- 明确一次性任务、可重复任务和重复发布失败时的玩家可见提示。
- 在 `TaskSystem` 低层与 `GameSession` 正式入口统一执行策略。
- 验收：重复发布、重复派遣、已完成任务重开和可重复任务四类边界均有测试。

## 每个 V4 Task 的交付闸门

①只执行一个 Task，并更新 `task_plan.md`、`progress.md`、`findings.md`。  
②修改脚本通过 Godot 4.7.2 `--check-only --script`。  
③专项测试、受影响回归、引用搜索和 `git diff --check` 全部通过。  
④无法确认的内容标记 `UNVERIFIED`；未实现内容标记 `NOT IMPLEMENTED`。  
⑤每个 Task 只创建一个本地完成提交；推送远端前取得 Jackie 明确授权。

## 环境记录

- Godot 可执行文件：`D:\steam\steamapps\common\Godot Engine\godot.windows.opt.tools.64.exe`
- 当前已知环境告警：`user://logs/godot.log` 写入失败、根证书仓库读取失败；需在目标机器复核。
